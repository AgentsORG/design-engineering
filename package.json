{
  "name": "design-engineering",
  "version": "2.3.0",
  "private": true,
  "type": "module",
  "description": "eve runtime for the design-engineering skill graph — a durable design-engineering agent with nine specialist subagents.",
  "scripts": {
    "sync:skills": "node scripts/sync-skills.mjs",
    "build:registry": "node scripts/build-registry.mjs",
    "dev": "npm run sync:skills && eve dev",
    "info": "eve info",
    "eval": "npm run sync:skills && eve eval"
  },
  "devDependencies": {
    "eve": "^0.31.3",
    "typescript": "^5.6.0"
  }
}
