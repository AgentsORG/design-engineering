---
name: scan-ai-tells
description: Scan UI or marketing copy for AI-default tells and content-authenticity misses — deletion list, not a redesign brief.
---

# Scan AI tells

Spawn or emulate the **anti-pattern-scanner** subagent (`agents/anti-pattern-scanner.md`).

1. Load `skills/design-engineering/references/anti-patterns/ai-default-tells.md` and `content-authenticity.md`.
2. Load `gotchas.md` and `pov.md` — installer hard rules trump the catalog.
3. Return a prioritized deletion list: what to remove or replace, each tied to a `[[wikilink]]`.
4. Do not propose new visual direction unless the user asks after the scan.
